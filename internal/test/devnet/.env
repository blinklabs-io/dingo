# Default docker compose profile for the devnet. `dingo` brings up the
# all-dingo network (three producers + relay). Override with
# COMPOSE_PROFILES=conformance (or the scripts' --conformance flag) for the
# dingo + cardano-node compatibility/conformance network.
COMPOSE_PROFILES=dingo
